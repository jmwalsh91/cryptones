import { Meta } from '@storybook/react'

import ActionToggle from '../formComponents/ActionToggle'

const meta: Meta = {
  title: 'Action Toggle',
  component: ActionToggle,
}

export default meta

export const Default = () => <ActionToggle />
