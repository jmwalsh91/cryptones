import { Meta } from '@storybook/react'

import Display from '../surfaces/Display'

const meta: Meta = {
  title: 'display',
  component: Display,
}

export default meta

export const Default = () => <Display />
