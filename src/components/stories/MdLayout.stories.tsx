import { Meta } from '@storybook/react'

import MdLayout from '../layout/MdLayout'

const meta: Meta = {
  title: 'Md Layout',
  component: MdLayout,
}

export default meta

export const Default = () => <MdLayout />
