import { Meta } from '@storybook/react'

import TrackIndicator from '../formComponents/TrackIndicator'

const meta: Meta = {
  title: 'Track Indicator',
  component: TrackIndicator,
}
export default meta

export const Default = () => <TrackIndicator />
