import { Meta } from '@storybook/react'

import Display from '../Display/Display'

const meta: Meta = {
  title: 'display',
  component: Display,
}

export default meta

export const Default = () => <Display />
